export const state = { user: null, subjects: [], route: "dashboard", eduAIContext: null };
export function setUser(user) { state.user = user; }
export function setSubjects(subjects) { state.subjects = subjects; }
